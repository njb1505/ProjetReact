import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App/>,
  document.getElementById('root')
);


/**
 1. On fait nos importations comme d'habitude
 2. On utilise la méthode render avec le ReactDom
 3. render prend deux arguments: un composant react et un element html. 
 */ 